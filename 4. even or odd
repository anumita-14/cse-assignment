#include <stdio.h>
#include<conio.h>
int main() {
    int num;
    printf("Enter a number:");
    scanf("%d",&num);
    if(num%2==0)
printf("It is an even number");
else
printf("It is an odd number");
getch();
    return 0;
}
