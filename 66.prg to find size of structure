#include <stdio.h>

struct Example {
    int a;
    char b;
    double c;
    float d;
};

int main() {
    struct Example e;
    printf("Size of Example structure: %lu bytes\n", sizeof(e));
    return 0;
}
