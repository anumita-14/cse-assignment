#include<stdio.h>
#include<conio.h>
int main()
{
int num,sum=0,rem;
printf("Enter a number:");
scanf("%d",&num);
while(num!=0)
{
rem=n%10;
sum+=rem;
n/=10;
}
printf("Sum of digits of a number: %d",sum);
getch();
return 0;
}
